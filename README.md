# 배달 앱 백엔드 시스템

Flask 기반의 배달 앱 백엔드 시스템입니다. 사용자(고객), 사장, 라이더 3가지 역할을 지원하는 배달 플랫폼입니다.

## 🚀 시작하기

### 필수 요구사항
- Python 3.8 이상
- MySQL 8.0 이상
- pip

### 설치 방법

1. **저장소 클론 및 디렉토리 이동**
```bash
cd database
```

2. **가상환경 생성 및 활성화**
```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
```

3. **의존성 설치**
```bash
pip install -r requirements.txt
```

4. **환경 변수 설정**
`.env` 파일을 생성하고 다음 내용을 추가하세요:
```
SECRET_KEY=your-secret-key-here
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=delivery_db
```

5. **데이터베이스 생성**
MySQL에서 데이터베이스를 생성하고 `create.sql` 파일을 실행하세요:
```bash
mysql -u username -p database_name < create.sql
```

6. **애플리케이션 실행**
```bash
python app.py
```

서버가 `http://localhost:5000`에서 실행됩니다.

## 📁 프로젝트 구조

```
database/
├── app.py                 # Flask 애플리케이션 메인 파일
├── config.py              # 설정 파일
├── models.py              # SQLAlchemy 모델 정의
├── requirements.txt       # Python 패키지 의존성
├── create.sql            # 데이터베이스 스키마
├── FEATURES.md           # 기능 목록 문서
├── routes/               # 라우터 Blueprint
│   ├── __init__.py
│   ├── users.py          # 사용자 라우터
│   ├── owners.py         # 사장 라우터
│   ├── riders.py         # 라이더 라우터
│   ├── stores.py         # 가게 라우터
│   ├── customer.py       # 고객 라우터
│   ├── favorites.py      # 찜하기 라우터
│   ├── reviews.py        # 리뷰 라우터
│   ├── payments.py       # 결제수단 라우터
│   └── coupons.py        # 쿠폰 라우터
├── templates/            # Jinja2 템플릿
│   ├── base.html
│   ├── firstpage.html
│   ├── signup.html
│   ├── main.html
│   ├── storelist2.html
│   ├── storedetail.html
│   ├── order.html
│   ├── orderlist2.html
│   ├── like.html
│   ├── payment.html
│   ├── owner.html
│   ├── rider.html
│   └── setting.html
└── utils/                # 유틸리티 함수
    └── auth.py           # 인증 관련 함수
```

## 📋 주요 기능

### 사용자 (User)
- 회원가입/로그인
- 사용자 정보 조회
- 주소 수정

### 사장 (Owner)
- 사장 회원가입/로그인
- 가게 등록
- 결제 수단 관리
- 쿠폰 관리

### 라이더 (Rider)
- 라이더 등록
- 라이더 정보 조회

### 고객 (Customer)
- 카테고리 조회
- 가게 목록 조회
- 메뉴 조회
- 주문 생성

### 기타 기능
- 찜하기 (Favorite)
- 리뷰 (Review)
- 결제 수단 (Payment)
- 쿠폰 (Coupon)

## 🔌 API 엔드포인트

총 30개의 API 엔드포인트가 구현되어 있습니다. 자세한 내용은 `FEATURES.md` 파일을 참조하세요.

### 주요 엔드포인트 예시

- `POST /users/register` - 회원가입
- `POST /users/login` - 로그인
- `GET /customer/categories` - 카테고리 목록
- `GET /customer/categories/{category_id}/stores` - 카테고리별 가게 목록
- `POST /customer/orders` - 주문 생성
- `POST /favorites` - 찜하기 추가
- `POST /reviews` - 리뷰 작성

## 🛠️ 기술 스택

- **프레임워크**: Flask 3.0.0
- **데이터베이스**: MySQL 8.0
- **ORM**: SQLAlchemy 2.0.23
- **템플릿 엔진**: Jinja2 3.1.2
- **인증**: Flask Session 기반

## 📝 참고사항

- 데이터베이스 연결 정보는 `.env` 파일에서 관리합니다.
- 프로덕션 환경에서는 `SECRET_KEY`를 반드시 변경하세요.
- 데이터베이스 테이블은 애플리케이션 실행 시 자동으로 생성됩니다 (`db.create_all()`).

## 📄 라이선스

이 프로젝트는 교육 목적으로 제작되었습니다.

